# Disable the "core" postgres plugins
disablePlugins:
  - lando-postgres

# Make sure we are dogfooding this plugin for testing purposes
plugins:
  "@lando/postgres": /home/runner/work/postgres/postgres

# Disable metrics reporting
stats:
  - report: false
    url: https://metrics.lando.dev
